# Copy to .env (repo root, gitignored) and fill in real values for local development.
# start.sh sources this before running the backend. Never commit the real .env.

SPRING_MAIL_USERNAME=your-gmail-address@gmail.com
SPRING_MAIL_PASSWORD=your-gmail-app-password
ADMIN_TOKEN=replace-with-a-freshly-generated-random-token
