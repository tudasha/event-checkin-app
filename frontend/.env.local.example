# Copy this file to .env.local and fill in real values for local development.
# .env.local itself is gitignored — never commit real credentials here.
VITE_API_URL=http://localhost:8080
VITE_ADMIN_TOKEN=replace-with-a-freshly-rotated-admin-token
